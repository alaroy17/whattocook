/**
 * Настройки сборки.
 *
 * Client ID здесь лежит открыто намеренно: в OAuth это публичный идентификатор,
 * он и так уходит в запросе к Google из браузера каждого пользователя. От чужого
 * использования его защищает не секретность, а список Authorized JavaScript origins
 * в Google Cloud — токен выдаётся только страницам с разрешённых адресов.
 *
 * Секретом является client secret, и он приложению не нужен: браузерный поток
 * его не использует. Не добавляйте его сюда.
 *
 * Значение можно переопределить переменной сборки VITE_GOOGLE_CLIENT_ID,
 * а на конкретном устройстве — полем в настройках приложения.
 */
export const GOOGLE_CLIENT_ID =
  import.meta.env.VITE_GOOGLE_CLIENT_ID?.trim() ||
  '66230170867-eqsrtr898m6q7nqpn9a6b8spqrqls2jp.apps.googleusercontent.com'
